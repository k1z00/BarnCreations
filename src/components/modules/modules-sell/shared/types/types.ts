export interface House {
    id: string
    title: string
    text: string
    src: string
    price: number
    floors: number
    area: number
    bedrooms: number
}

export interface HouseModal {
    title: string
    price: number
}